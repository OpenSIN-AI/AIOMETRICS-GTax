# Context Fulltext

- source_path: DB.md
- source_sha256: 23d842336e93675a6e160970df3fa583bc42b29104a9064a4a00811de50c3081
- chunk: 1/1

```text
# Datenbank Dokumentation - belege.db

## Übersicht

Diese Dokumentation beschreibt die Struktur der `belege.db` SQLite-Datenbank, die zur Speicherung von Einnahmen- und Ausgabenbelegen der Firma Zoe Solar (Jeremy Schulze) verwendet wird. Die Datenbank dient als zentrale Knowledge Base für alle relevanten Finanzdaten, die aus PDF- und Bilddokumenten extrahiert werden.

## Datenbank: `belege.db`

### Tabelle: `belege`

Diese Tabelle speichert detaillierte Informationen zu jedem verarbeiteten Beleg.

| Spaltenname        | Datentyp | Beschreibung                                                                |
| :----------------- | :------- | :-------------------------------------------------------------------------- |
| `id`               | INTEGER  | Eindeutiger Primärschlüssel, automatisch inkrementierend.                   |
| `filename`         | TEXT     | Originaler Dateiname des Belegs (z.B. `rechnung_123.pdf`). Eindeutig.     |
| `original_path`    | TEXT     | Vollständiger Pfad zur Originaldatei des Belegs.                          |
| `category`         | TEXT     | Klassifizierung des Belegs: `EINNAHME`, `AUSGABE`, `UNBEKANNT`.           |
| `sender_name`      | TEXT     | Name des Absenders (Lieferant bei Ausgaben, Zoe Solar bei Einnahmen).     |
| `sender_address`   | TEXT     | Adresse des Absenders.                                                      |
| `sender_tax_id`    | TEXT     | Steuernummer des Absenders.                                                 |
| `sender_vat_id`    | TEXT     | Umsatzsteuer-Identifikationsnummer (USt-IdNr.) des Absenders.             |
| `receiver_name`    | TEXT     | Name des Empfängers (Zoe Solar bei Ausgaben, Kunde bei Einnahmen).        |
| `receiver_address` | TEXT     | Adresse des Empfängers.                                                     |
| `receiver_tax_id`  | TEXT     | Steuernummer des Empfängers.                                                |
| `receiver_vat_id`  | TEXT     | Umsatzsteuer-Identifikationsnummer (USt-IdNr.) des Empfängers.            |
| `invoice_number`   | TEXT     | Rechnungs- oder Belegnummer.                                                |
| `invoice_date`     | TEXT     | Datum der Rechnung/des Belegs (Format: YYYY-MM-DD).                       |
| `due_date`         | TEXT     | Fälligkeitsdatum der Zahlung (Format: YYYY-MM-DD).                        |
| `net_amount`       | REAL     | Nettobetrag des Belegs.                                                     |
| `tax_amount`       | REAL     | Steuerbetrag (z.B. Mehrwertsteuer).                                         |
| `total_amount`     | REAL     | Gesamtbetrag des Belegs (Brutto).                                           |
| `currency`         | TEXT     | Währung des Betrags (z.B. `EUR`).                                           |
| `payment_method`   | TEXT     | Verwendete Zahlungsmethode (z.B. `Mastercard`, `Bar`, `Überweisung`).     |
| `description`      | TEXT     | Kurze Beschreibung des Beleginhalts.                                        |
| `line_items`       | TEXT     | Detaillierte Auflistung der Posten als JSON-String.                        |
| `additional_data`  | TEXT     | Zusätzliche, nicht standardisierte Daten als JSON-String.                 |
| `raw_text_content` | TEXT     | Vollständiger, extrahierter Textinhalt des Belegs (OCR und/oder direkt).  |
| `analysis_notes`   | TEXT     | Notizen zur Analyse und Klassifizierung des Belegs.                       |

---

## Best Practices Februar 2026

Diese Datenbankstruktur entspricht den Best Practices für die Belegverwaltung im Februar 2026, indem sie:
*   **Vollständigkeit:** Alle relevanten Felder für Einnahmen und Ausgaben abdeckt.
*   **Flexibilität:** `line_items` und `additional_data` als JSON-Felder ermöglichen die Speicherung komplexer und variabler Daten.
*   **Nachvollziehbarkeit:** `original_path` und `raw_text_content` gewährleisten die Rückverfolgbarkeit zum Originaldokument und zum extrahierten Roh-Text.
*   **Standardisierung:** Klare Datentypen und Beschreibungen erleichtern die Integration in andere Systeme und die automatisierte Verarbeitung.
*   **KI-Readiness:** Die Struktur ist darauf ausgelegt, von KI-Agenten befüllt und analysiert zu werden, um eine hohe Datenqualität zu gewährleisten.

```
